#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("kerem a hatvany alapot es  a kitevot: ");
    int a,b;
    if(scanf("%d,%d", &a,&b)!=2){
        printf("Hibas adatok!");
        return -1; // ez jelenti a hibás végeredményt
        while((c= getchar())!= '!n'); //puffer törlés
    }
    return 0;
}
