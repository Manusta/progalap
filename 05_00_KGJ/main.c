#include <stdio.h>
#include <stdlib.h>

int main()
{
    int tomb [10];
        for(int i = 0; i<tomb; i++){
            tomb[i]=i;
        }
    printf("a %d. elem: %d/n", i, tomb[i]);
    }
    return 0;
}
